console.log('** Welcome to advanced data structures and algorithms ...**');
console.log(`Searching refers to iterating over the data structure’s
elements to retrieve some data. Sorting refers to putting the data structure’s
elements in order.`);
/** Searching refers to iterating over the data structure’s elements to retrieve some data.
 *  Sorting refers to putting the data structure’s elements in order.
 */
